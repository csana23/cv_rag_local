[project]
name = "chromadb_etl"
version = "0.1"
dependencies = [
   "PyPDF2",
   "retry",
   "langchain_community",
   "langchain",
   "unstructured",
   "chromadb",
   "ollama"
]

[project.optional-dependencies]
dev = ["black", "flake8"]